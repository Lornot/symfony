{% extends 'base.html.twig' %}

{% block title %}
    {{ idea_title }}
{% endblock %}

{% block body %}
    <h1>{{ idea_title }}</h1>
    <p>{{ idea_description }}</p>
    <p>{{ idea_created_at }}</p>
    <a href="{{ path('ideas_update', {'idea' : idea_id }) }}">Update</a>
{% endblock %}